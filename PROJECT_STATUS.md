# حالة المشروع — روضة ماما زينب

> **لأي محادثة جديدة تكمل هذا المشروع:** اقرأ هذا الملف فقط قبل البدء.
> لا تعِد قراءة تاريخ المحادثة الأصلية ولا كل ملفّات الكود — هذا الملف
> كافٍ لمعرفة أين توقّفنا. حدِّثه في نهاية كل جلسة عمل جديدة (قسم
> "السجلّ الزمني" في الأسفل + أي تغيير في "الحالة الحالية").

آخر تحديث: بعد جولة «الهاتف أوّلاً» — صورة البطل الجديدة، العرض التحريري للأنشطة، والتأثيرات الراقية (السجلّ الزمني، البند 13).

---

## 1. ما هو المشروع

صفحة هبوط بصفحة واحدة لإعلان ممول على ميتا، لروضة أطفال في تاسوست
(جيجل، الجزائر) — الهدف الوحيد: تعبئة استمارة "اطلب مكالمة". مبنيّة
بجانبها منصّة إدارة داخلية (`/dashboard`) لرؤية التسجيلات ومتابعتها.

- **الموقع الحيّ:** https://mama-zayneb.vercel.app
- **منصّة الإدارة:** https://mama-zayneb.vercel.app/login
- **المستودع:** github.com/abdourhman51-crypto/mama-zayneb-
- **الفرع:** `claude/mama-zayneb-landing-cmuzvw`
- **Vercel:** مشروع `mama-zayneb` (فريق `adammm`)
- **Supabase:** مشروع `mama-zayneb`، project_id: `vpinaehjfflnrlpfxlko`، منطقة eu-west-3

⚠️ **لا تلمس أي شيء باسم "adam"** — مستودعات ومشاريع Vercel أخرى في نفس
الحساب تخصّ مشروعاً مختلفاً تماماً وليست لنا.

---

## 2. الحالة الحالية (ماذا يعمل، ماذا لا)

### يعمل بالكامل ومنشور على الإنتاج
- صفحة الهبوط الكاملة: Hero + قمع تسويقي (الألم ← التحوّل ← الأنشطة ←
  الطمأنينة ← أسئلة ← استمارة) + تذييل. الهوية البصرية مطبَّقة بالكامل.
- الاستمارة: 5 حقول، تحقّق من رقم هاتف جزائري، honeypot، حالات
  تحميل/نجاح/خطأ. تُدرَج في Supabase عبر `/api/lead` (يعمل حتى بدون
  `SUPABASE_SERVICE_ROLE_KEY` — يستعمل المفتاح العام كبديل).
- منصّة الإدارة `/dashboard`: دخول برقم الهاتف، شريط جانبي قابل للطيّ
  مع أيقونات، صفحة تسجيلات كاملة (بحث، تصفية، مؤشّرات، تحديث حالة)،
  4 وحدات مقفلة (CRM, HR, مالية, تقارير) بصفحات تشرح ما ستفعله.
  محمية بـ`middleware.ts` + جدول `staff` (RLS عبر `is_staff()`).
- بيكسل ميتا (تحميل مؤجَّل بعد أول تفاعل) + **Meta Conversions API**
  من الخادم (نفس `event_id` للدمج بين القناتين — راجع `lib/capi/`).
- إشعارات فورية (Web Push) + تحديث لحظي (Supabase Realtime) في اللوحة.
- دعوة تثبيت تلقائية على آيفون (PWA) لمرّة واحدة.

### يحتاج إعداداً من المالك (متغيّرات بيئة في Vercel) — لم تُضَف بعد
هذه اختيارية وكل شيء يعمل بدونها (تُتخطّى بصمت)، لكنها تحسّن الأداء:

| المتغيّر | لماذا | من أين |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` | إدراج أضمن (يتجاوز RLS) + الإشعارات الفورية تحتاجه لقراءة الاشتراكات | Supabase → Project Settings → API Keys |
| `VAPID_PRIVATE_KEY` | تفعيل Web Push الفعلي (المفتاح العام مكتوب بالكود مسبقاً) | مولَّد مسبقاً: `M8NeY4HrfC7EfjdivPtBl5OErT2efOpv2mAM80lIarQ` |
| `META_CAPI_ACCESS_TOKEN` | تفعيل Conversions API | Meta Events Manager → Settings → Conversions API → Generate access token |
| `META_CAPI_TEST_EVENT_CODE` | اختياري، للتحقّق عبر Test Events فقط، يُحذف بعد الاختبار | Meta Events Manager → Test Events |

**⚠️ مفتاح `service_role` القديم مكشوف في تاريخ المحادثة — المستخدم
لصقه نصّاً في الشات.** يجب تدويره من Supabase → Settings → API Keys →
Rotate قبل استعماله في أي مكان، إن لم يكن قد فُعل ذلك بعد.

### حسابات الدخول للوحة (Supabase Auth، جدول `staff`)
الدخول بـ**رقم الهاتف** لا بالبريد (يُحوَّل داخلياً لهوية
`<رقم>@staff.mamazayneb.dz` — راجع `lib/staffAuth.ts`).

| الرقم | كلمة المرور | الدور |
|---|---|---|
| `0771888841` | `mamaz123` | owner (المالكة الحقيقية) |
| `0770000001` | `Mz-Adam-4718!` | admin (رقم مؤقّت — يجب استبداله برقم حقيقي) |

### معروف أنه غير مختبَر فعلياً (حدود بيئة العمل)
كل جلسات العمل حتى الآن **لا تملك اتصال شبكة بـ`supabase.co` أو
`vercel.app`** (البروكسي يحجبها) — لا يمكن اختبار تسجيل الدخول الفعلي
أو استدعاءات API الحيّة من هذه البيئة. كل الإصلاحات تحقّقنا منها
بالبناء المحلي (`npx next build`) وبمنطق الكود، لا بتشغيل حيّ. أي
جلسة قادمة يجب أن تطلب من المستخدم نفسه اختبار الميزات الحسّاسة
(الدخول، الإشعارات، CAPI) على جهازه الحقيقي.

---

## 3. قرارات معمارية مهمّة (لماذا كذا لا غيره)

- **كل النصوص في ملفّين**: `content/site.ts` (صفحة الهبوط) و
  `content/dashboard.ts` (منصّة الإدارة). أي تعديل نصّي يبدأ من هناك.
- **قاعدة عدم التكرار**: الأعمار/التوقيت/النقل تُذكر مرّة واحدة فقط
  (بطاقات الـHero + الأسئلة الشائعة). لا تُكرَّر في أقسام أو التذييل.
- **لا ادّعاءات غير مؤكَّدة — لكن المؤكَّد يُقال كاملاً**:
  القاعدة هي عدم اختراع معلومة، لا حذف الحقائق. المالكة أكّدت لاحقاً
  قائمة طويلة من الحقائق الحقيقية، فأُدرجت كلّها في النصّ.

  ⚠️ **تنبيه لأي جلسة قادمة:** نسخة قديمة من هذا الملف كانت تقول إن
  «الكاميرات والترخيص والوجبات» ادّعاءات غير مؤكَّدة وتم حذفها. **هذا
  خطأ ولم يعد صحيحاً.** لا تحذفها مجدداً.

  **الحقائق المؤكَّدة من المالكة (كلّها مستعمَلة في `content/site.ts`):**
  - الأعمار: 6 أشهر–5 سنوات، موزّعة على **3 أقسام** لكلٍّ برنامجه
  - التوقيت: 8:00–16:30
  - النقل: تاسوست + الأمير عبد القادر + أولاد صالح + السانكيام
  - **16 كاميرا** — 4 كاميرات في كل قسم، واحدة لكل زاوية
  - **مرخّصة ومعتمدة رسمياً**
  - **12 مربّية** لكلٍّ شهادتها وخبرتها + **مختصّة أرطفونية** داخل الروضة
  - **3 وجبات** ببرنامج غذائي مصادَق عليه من طبيب الروضة + احترام الحساسيات
  - جناح نوم مستقلّ للقيلولة · حديقتان + فضاء لعب · عاملة نظافة بدوام كامل
  - قانون داخلي مكتوب لتسليم الطفل + مربّية دائمة في الاستقبال
  - فترة تأقلم: من يومين إلى شهر حسب الطفل
  - تخفيض للإخوة · أنشطة أسبوعية (بحر، شواء، الحماية المدنية، ثقافية ودينية)
  - شهادتا وليّ أمر حقيقيتان (مذكورتان نصّاً في قسم `trust`)

  **ما يبقى خارج الصفحة عمداً:** السعر — لا يُذكر إطلاقاً. الصفحة
  تراكم القيمة والمكالمة تكشف الرقم (نفس الرقم بلا سياق يبدو مرتفعاً).
- **⚠️ قيد الصور: لا يظهر وجه أي طفل أو مربّية في أي صورة.**
  كل الصور في `public/images/` مقصوصة من مصادرها الأصلية لتحقيق هذا
  (أطفال من الخلف، أيدٍ أثناء النشاط، شعار الروضة على القمصان). أي
  صورة جديدة تُضاف يجب أن تحترم القيد نفسه. لا تُعِد صورة بوجه ظاهر.
- **الحركة: بلا مكتبات.** `components/Reveal.tsx` مبنيّ يدوياً
  (‏<1KB) لأن كل بدائل 21st.dev تعتمد Motion‏ (+40KB) والقاعدة
  المتّفق عليها: إن تعارضت الحركة مع السرعة، السرعة تفوز. لا تستبدله
  بمكتبة. transform وopacity فقط · IntersectionObserver ·
  إيقاف كامل مع prefers-reduced-motion · المحتوى يظهر بلا جافاسكربت.
- **رقم الأماكن في متغيّر واحد**: `fomo.seatsLeft` في
  `content/site.ts`. لا تكرّره في نصّ آخر.
- **استراتيجية نصّ صفحة الهبوط (الصيغة النهائية — لا تُعكَس بلا سبب)**:
  1. الصفحة تستقبل **ثلاث زوايا إعلانية** معاً: الأمّ العاملة، تأخّر
     النطق والتطوّر، والخجل والاندماج. قسم `pain` يلتقط الثلاثة حتى لا
     يشعر أي زائر قادم من أي إعلان أنه في المكان الخطأ.
  2. **تدفّق الفجوة الفضولية**: كل قسم يفتح سؤالاً يجيب عنه القسم الذي
     يليه (`ctaNote` في كل قسم مصمَّم لذلك)، فلا يجد القارئ نقطة توقّف.
  3. **السعر لا يُذكر إطلاقاً** — الصفحة تراكم القيمة والمكالمة تكشف الرقم.
  4. الـCTA صيغته «اترك رقمك ونتصل بك» لا «احجز زيارة» — الالتزام أخفّ.
  الشرح مكتوب أيضاً في رأس `content/site.ts` نفسه.
- **الدخول للوحة بالهاتف بلا مزوّد SMS**: حيلة `<رقم>@staff.mamazayneb.dz`
  كهوية بريد وهمية عند Supabase Auth — تتجنّب تكلفة اشتراك SMS.
- **جدول `staff` كقائمة صلاحيات منفصلة عن Auth**: التسجيل في Supabase
  Auth وحده لا يمنح أي وصول؛ يجب أن يكون `user_id` مُدرَجاً في
  `public.staff` (سياسات RLS تستعمل `is_staff()`).
- **الوحدات المقفلة (CRM/HR/مالية/تقارير) هي "hooks" مقصودة**: تصميم
  مقصود لبناء توقّع المالِكة ليستثمر صاحب المشروع في المراحل القادمة —
  معاينة صمّاء بلا بيانات وهمية + نص "الأساس مبنيّ من الآن" + جملة
  ختامية (`hook`) تبني الحافز. لا تحذفها ولا "تُكملها" ببيانات مزيَّفة.
- **بنية قابلة للتوسّع لتصبح CRM/HR/مالية لاحقاً**: كل تسجيل يُخزَّن
  ببنية (`utm_source`, `utm_campaign`, `status`) جاهزة لتُستهلَك من
  الوحدات القادمة دون إعادة هيكلة.
- **CAPI + Pixel معاً بنفس `event_id`**: لمنع الاحتساب المزدوج (راجع
  قسم "Conversions API" في `README.md`).

---

## 4. خريطة الملفّات السريعة

```
content/site.ts          نصوص صفحة الهبوط (كل شيء هنا)
content/dashboard.ts     نصوص منصّة الإدارة + تعريف الوحدات (modules[])
app/page.tsx             ترتيب أقسام صفحة الهبوط
app/dashboard/           منصّة الإدارة (page.tsx = التسجيلات، crm|hr|finance|reports = مقفلة)
app/login/               دخول اللوحة برقم الهاتف
app/api/lead/route.ts    الاستمارة → Supabase + إشعار فوري + CAPI
app/api/push/subscribe/  حفظ اشتراك إشعارات جهاز
components/dashboard/    Shell (الشريط الجانبي)، PushCard، LiveLeads، IosInstallPrompt، …
lib/phone.ts             تحقّق رقم جزائري
lib/pixel.ts             بيكسل ميتا + قراءة كوكيز fbp/fbc
lib/capi/                Meta Conversions API (hash.ts, send.ts)
lib/push/                إشعارات فورية (config, client, send)
lib/supabase/            عملاء Supabase (browser/server) + middleware
lib/staffAuth.ts         تحويل رقم الهاتف لهوية دخول
middleware.ts            حماية /dashboard
supabase/migrations/     leads, staff, push_subscriptions + كل السياسات
README.md                التوثيق الكامل التفصيلي (كل شيء مشروح فيه بعمق)
```

**للتفاصيل الكاملة عن كل ميزة (كيف تعمل، كيف تُعدَّل، لماذا صُمِّمت
هكذا) اقرأ `README.md`** — هذا الملف (`PROJECT_STATUS.md`) للمُلخَّص
واستئناف العمل فقط.

---

## 5. المهام المتبقّية / المقترَحة (لم تُطلَب بعد، أفكار للمستقبل)

- [ ] استبدال رقم `0770000001` (admin) برقم حقيقي.
- [ ] إضافة `SUPABASE_SERVICE_ROLE_KEY` و`VAPID_PRIVATE_KEY` في Vercel.
- [ ] الحصول على `META_CAPI_ACCESS_TOKEN` وتفعيله + اختباره عبر Test Events.
- [ ] حذف التسجيلات التجريبية إن وُجدت (تحقّق من جدول `leads` في Supabase).
- [ ] بناء وحدة CRM الفعلية (`app/dashboard/crm`) — البنية جاهزة، لم يُطلَب البدء بعد.
- [ ] صورة OG للمشاركة على فيسبوك/واتساب (`app/opengraph-image.png`).
- [x] ~~شهادات الأولياء~~ — أُدرجت شهادتان حقيقيتان داخل قسم `trust`.
- [ ] صور إضافية لأنشطة أخرى إن وُجدت.
- [ ] تأكيد أيام العمل (التوقيت معروف، الأيام لا).
- [ ] «بقيت 10 أماكن» — الرقم الآن في `fomo.seatsLeft` بـ`content/site.ts`.
      يجب تحديثه أو حذف شريط الإلحاح حين تمتلئ الأماكن حتى لا يتحوّل
      إلى ادّعاء كاذب.
- [ ] **صورة الـHero المطلوبة لم تصل**: أطفال من الخلف على العشب أمام
      البحر بقمصان الروضة البيضاء. الحالي (`hero-shirt.webp`) بديل
      مؤقّت مقصوص من صورة الرسم — يحترم قيد «لا وجوه» ويُظهر الشعار
      على القميص. استبدله حين تصل الصورة الحقيقية.
- [ ] صور رحلات جديدة (بحر، شواء، الحماية المدنية) لقسم «المرافق
      والرحلات» — القسم نصّي حالياً لأن كل الصور مستعمَلة في «الأنشطة».

---

## 6. السجلّ الزمني (جلسات العمل، من الأقدم للأحدث)

كل بند = جلسة عمل واحدة تقريباً. أضِف بنداً جديداً في الأسفل عند
انتهاء أي جلسة مستقبلية، ولا تُعدِّل ما فوقه.

1. **الإنشاء الأولي**: مستودع GitHub + مشروع Vercel + مشروع Supabase
   جديدة تماماً. صفحة هبوط أولى بالهوية البصرية والنصوص الأصلية.
2. **مراجعة النصوص والصور**: حذف كل الادّعاءات غير المؤكَّدة، إعادة
   صياغة كقمع تسويقي مبني على نقاط ألم حقيقية (إدمان الشاشة، الطاقة
   المكبوتة، الخجل)، دمج صور أنشطة حقيقية جديدة (خيل، حرفة، اكتشاف).
   إصلاح تلاشي الـHero ليُظهر شعار الروضة على القميص قبل التلاشي.
3. **ربط بيكسل ميتا**: معرّف `2615027122286387` مكتوب كقيمة افتراضية
   في الكود (متغيّر البيئة يتقدّم عليه إن وُجد).
4. **بناء منصّة الإدارة v1**: `/login` + `/dashboard` بمصادقة Supabase،
   صفحة تسجيلات بسيطة، 4 وحدات مقفلة. إصلاح ثغرة أمنية (RLS كانت تسمح
   لأي مسجِّل حساب برؤية كل التسجيلات — أُضيف جدول `staff`).
5. **إصلاح انهيار الخادم**: `statusTone` كانت مُصدَّرة من ملف `'use
   client'` ومستدعاة من مكوّن خادم → نُقلت لملفّ محايد `lib/leadStatus.ts`.
6. **إعادة بناء اللوحة بمعايير احترافية**: شريط جانبي قابل للطيّ
   بأيقونات lucide-react، دخول بالهاتف بدل البريد (حيلة النطاق الوهمي).
7. **إشعارات فورية + تحديث لحظي**: Web Push (VAPID) + Supabase
   Realtime. حذف البيانات التجريبية.
8. **إصلاح أعطال حقيقية بعد بلاغ المستخدم**: بطاقة الإشعار كانت تُخفي
   نفسها بنجاح وهمي (لا تتحقّق من نجاح الحفظ الفعلي) — أُصلحت
   بـ`hasActiveSubscription()` ومقارنة مفتاح VAPID. التحديث اللحظي كان
   يفشل بصمت بسبب سباق توقيت (القناة تُفتَح قبل جهوز الجلسة) — أُصلح
   بانتظار `getSession()`. أُضيف `.tap-feedback` (استجابة فورية بـCSS)
   و`loading.tsx` لكل قسم (هيكل بصري فوري) لحل شكوى بطء اللوحة. أُضيفت
   دعوة تثبيت تلقائية لمرّة واحدة على آيفون. حُذفت نصوص طلبها المستخدم
   صراحة ("الإشعارات مفعّلة"، "التحديث لحظي").
9. **تفعيل Meta Conversions API**: حدث `Lead` من الخادم مباشرة (ليس
   فقط من المتصفّح)، مع `event_id` مشترَك للدمج ومنع الاحتساب المزدوج،
   وبيانات مطابقة مجزَّأة (رقم الهاتف، IP، user-agent، fbp/fbc).
   حُذف الهيكل القديم غير المستعمل `app/api/capi/route.ts`.
10. **إنشاء هذا الملف** (`PROJECT_STATUS.md`) لحفظ التقدّم وتوفير
    التوكنز على الجلسات القادمة.
12. **إعادة بناء البصريات + أقسام إزالة الحواجز**: أقسام جديدة
    (المرافق والرحلات، الضمانات، الشهادات، شريط الإلحاح، أسطر
    الطمأنة فوق الحقول). كشف هادئ عند التمرير مبنيّ يدوياً
    (`components/Reveal.tsx`) بدل مكتبة Motion — ‏1KB بدل 40KB.
    **قيد جديد دائم: لا يظهر وجه أي طفل أو مربّية في أي صورة** —
    أُعيد قصّ كل الصور من مصادرها الأصلية لتحقيقه.

11. **النسخة النهائية لنصوص صفحة الهبوط**: استبدال `content/site.ts`
    بالكامل بصيغة كتبها المستخدم — ثلاث زوايا إعلانية، تدفّق فجوة
    فضولية، وإخفاء السعر عمداً. أُضيفت البراهين الحقيقية التي أكّدتها
    المالكة (16 كاميرا، ترخيص، 12 مربّية، مختصّة أرطفونية، 3 وجبات
    بمصادقة طبيب، جناح نوم، حديقتان، 3 أقسام، شهادتا وليّ أمر، تخفيض
    للإخوة، تغطية النقل الموسّعة). **صُحِّح قرار معماري خاطئ** في هذا
    الملف كان يصنّف الكاميرات والترخيص كادّعاءات غير مؤكَّدة ويأمر
    بحذفها — راجع القسم 3.

13. **جولة «الهاتف أوّلاً» + التأثيرات الراقية**:
    - **صورة البطل الجديدة** `public/images/hero-sea.webp`: بنات من
      الخلف على العشب أمام البحر واسم الروضة على ظهورهنّ — تحقّق قيد
      «لا وجوه» وتُظهر الهويّة في آن واحد.
    - **الأنشطة صارت عرضاً تحريرياً لا بطاقات** (`components/Activities.tsx`):
      كل صورة بنسبتها الطبيعية (`w`/`h` الحقيقيان في `content/site.ts`)
      فلا قصّ قسري يقطع الوجوه أو يشوّه الإطار. الهاتف: صورة كاملة
      العرض ثم النصّ تحتها. الحاسوب: تناوب 7/5. ⚠️ **لا تُعِد هذه
      الصور إلى بطاقات بنِسَب ثابتة — طُلِب صراحةً عكس ذلك مرّتين.**
    - **صورة مكتب الاستقبال** `public/images/reception.webp` داخل قسم
      المرافق بعرض تحريري (صورة + نصّ، لا نصّ فوق الصورة).
    - **`components/WordReveal.tsx`**: كشف عنوان البطل كلمةً كلمة.
      ⚠️ **كلمة لا حرفاً**: العربية خطّ متّصل، وتقسيمها إلى أحرف داخل
      عناصر منفصلة يكسر الوصلات. لا تحوّله إلى Typewriter حرفيّ.
    - **كاروسيل الشهادات بالسحب** (`components/SocialProof.tsx`):
      `scroll-snap` أصليّ + نقاط تنقّل، بلا مكتبة إيماءات.
    - **أكورديون متحرّك** (`components/Faq.tsx`): `grid-template-rows`
      من `0fr` إلى `1fr` — ارتفاع متحرّك بلا قياس بجافاسكربت، والنصّ
      يبقى في الـDOM دائماً (مفيد للسيو).
    - **الورقة السفلية** (`components/BottomSheet.tsx` + `lib/leadSheet.ts`):
      على الشاشات دون 1024px تفتح أزرار الدعوة (الشريط العلوي وشريط
      الإلحاح) ورقة بحقلين بدل القفز إلى أسفل الصفحة. التواصل بحدث
      مخصّص على `window` لا بسياق React. ⚠️ **لا تُضِف زرّاً عائماً
      ثالثاً** — يزاحم زرّ واتساب ويكرّر اللون الوردي.
    - كل الحركات في `app/globals.css` بـ`transform`/`opacity` فقط،
      ومعطّلة تحت `prefers-reduced-motion`. حجم صفحة الهبوط بقي
      ضمن حدود الأداء المتّفق عليها.

14. **زرّ حذف التسجيلات في اللوحة**: `components/dashboard/DeleteLeadButton.tsx`
    (نفس نمط `StatusSelect.tsx` — حذف مباشر عبر عميل Supabase +
    `router.refresh()`)، مع تأكيد `window.confirm` قبل الحذف. مُضاف في
    عمود جديد بجدول سطح المكتب وزرّ داخل بطاقة الهاتف. سياسة RLS جديدة
    `leads_staff_delete` (`for delete … using (is_staff())`) طُبِّقت على
    قاعدة الإنتاج عبر Supabase MCP وحُفظت أيضاً كملفّ migration
    (`supabase/migrations/20260908093400_leads_staff_delete_policy.sql`)
    — لم تكن هناك سياسة حذف من قبل. `components/dashboard/LiveLeads.tsx`
    كان يستمع فقط لـ`INSERT`/`UPDATE`؛ أُضيف `DELETE` أيضاً حتى يختفي
    التسجيل المحذوف لحظياً عند كل من يفتح اللوحة، لا فقط عند من ضغط
    الزرّ. **ملاحظة**: التسجيلات الجديدة كانت تظهر لحظياً بلا تحديث
    صفحة أصلاً (بند 7) — لم يكن هذا يحتاج إصلاحاً، فقط الحذف كان ناقصاً.
